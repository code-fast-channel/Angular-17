import { MultipleSearchPipe } from './multiple-search.pipe';

describe('MultipleSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MultipleSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
